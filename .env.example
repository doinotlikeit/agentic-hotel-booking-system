# ============================================
# Hotel Booking System - Environment Variables
# ============================================

# --------------------------------------------
# GCP Configuration (REQUIRED for ADK)
# --------------------------------------------

# Your Google Cloud Project ID
# Get this from: https://console.cloud.google.com/
GCP_PROJECT_ID=your-project-id-here

# GCP Region where VertexAI is available
# Common values: us-central1, us-east1, europe-west1
GCP_LOCATION=us-central1

# --------------------------------------------
# GCP Authentication (Choose ONE method)
# --------------------------------------------

# Method 1: Service Account (Recommended for production)
# Create service account and download key from:
# https://console.cloud.google.com/iam-admin/serviceaccounts
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Method 2: Application Default Credentials (Recommended for development)
# Run: gcloud auth application-default login
# No additional environment variable needed

# --------------------------------------------
# Application Configuration (Optional)
# --------------------------------------------

# Server port (default: 8080)
SERVER_PORT=8080

# ADK Model Configuration (default: gemini-2.0-flash-exp)
ADK_MODEL=gemini-2.0-flash-light

# ADK Temperature (default: 0.7, range: 0.0-1.0)
ADK_TEMPERATURE=0.7

# ADK Max Tokens (default: 2048)
ADK_MAX_TOKENS=2048

# WebSocket Allowed Origins (default: http://localhost:4200)
WEBSOCKET_ALLOWED_ORIGINS=http://localhost:4200

# ============================================
# Setup Instructions
# ============================================

# 1. Enable VertexAI API:
#    gcloud services enable aiplatform.googleapis.com

# 2. Set GCP_PROJECT_ID and GCP_LOCATION above

# 3. Choose authentication method:
#    - For local development: gcloud auth application-default login
#    - For production: Set GOOGLE_APPLICATION_CREDENTIALS

# 4. Copy this file to .env:
#    cp .env.example .env

# 5. Update values in .env with your actual configuration

# 6. Source the environment variables:
#    source .env
#    # OR for export format:
#    export $(cat .env | xargs)
